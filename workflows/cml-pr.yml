name: Test CML PR usage
on:
  workflow_dispatch:

#include "../includes/token.yml"
jobs:
  #define JOB_ID user-pr
  #include "../includes/setup.yml"
    #
      #define TEST_USER_EMAIL dabarnes2b@gmail.com
      #define TEST_USERNAME dacbd
      #include "../includes/tests/pr-user.yml"