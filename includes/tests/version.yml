#
  TEST_JOB_ID:
    needs: [JOB_ID]
    runs-on: [self-hosted, LABEL]
    steps:
      #include "../steps/base-checkout.yml"
      - name: Test
        run: |
          which cml
          cml --version
