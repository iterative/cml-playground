#
  #
    #
      - name: Modifiy file for PR
        run: |
          echo "# Test on $(date --iso-8601)" >> README.md
      - name: CML PR
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cml pr .
