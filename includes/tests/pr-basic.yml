#
  #
    #
      - name: Modifiy file for PR
        run: |
          echo "# Test on $(date --rfc-3339=ns)" >> README.md
      - name: CML PR
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cml pr .
