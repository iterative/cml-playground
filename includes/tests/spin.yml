#
  #
    #
      - name: Do work
        run: |
          echo "work for 30"
          timeout --preserve-status 30s bash -c 'md5sum /dev/zero'
          echo "work for 30"
          timeout --preserve-status 30s bash -c 'md5sum /dev/zero'
