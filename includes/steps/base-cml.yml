#
  #
    #
      #ifdef TEST_CML_BRANCH
      #include "./cml-branch.yml"
      #else
      #// https://github.com/iterative/setup-cml
      - uses: iterative/setup-cml@v1
      #ifdef TEST_CML_VERSION
        with:
          version: TEST_CML_VERSION
      #endif
      #endif